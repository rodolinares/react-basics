import { Input, Label, RoundedSlider } from "./style";

const RoundedSwitch = () => {
  return (
    <Label>
      <Input type="checkbox" />
      <RoundedSlider />
    </Label>
  );
};

export default RoundedSwitch;
